spring:
  application:
    name: pd-test-backend
    
  datasource:
    url: jdbc:mysql://localhost:3306/pd_test?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: rootpassword1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        
  redis:
    host: localhost
    port: 6379
    password: redispassword1234
    
server:
  port: 8080
  
logging:
  level:
    org.springframework.web: DEBUG
    com.test: DEBUG
